
examples = [
    {
        "query": "大管轮在备车时如果没对钟，评价标准是怎么扣分的？",
        "divided": [
            "大管轮需要考核哪些评估任务",
            "备车操作属于哪个具体任务",
            "该任务下关于对车钟的评价标准是什么"
        ],
        "graph": [
            ("大管轮", "[需考核科目]", "UNKNOWN 评估科目"),
            ("UNKNOWN 评估科目", "[包含评估任务]", "UNKNOWN 评估任务"),
            ("UNKNOWN 评估任务", "[评价标准]", "①与驾驶台联系对车钟")
        ]
    },
    {
        "query": "轮机长在主机故障分析时，如果没报告驾驶台会怎么样？",
        "divided": [
            "轮机长负责的故障分析任务是什么",
            "该任务的评估要素有哪些",
            "关于报告驾驶台的具体评价标准"
        ],
        "graph": [
            ("轮机长", "[需考核科目]", "《轮机模拟器》"),
            ("《轮机模拟器》", "[包含评估任务]", "1.1 主机故障分析及其排除"),
            ("1.1 主机故障分析及其排除", "[评价标准]", "①故障出现后...并视情报告驾驶台")
        ]
    },
    {
        "query": "二管轮吊缸拆装气缸盖需要准备哪些工具？",
        "divided": [
            "二三管轮的动力设备拆装考核科目",
            "气缸盖拆装任务的评估要素",
            "准备工作涉及的工具要求"
        ],
        "graph": [
            ("二/三管轮", "[需考核科目]", "《动力设备拆装》"),
            ("《动力设备拆装》", "[包含评估任务]", "1.1 气缸盖拆装与检查"),
            ("1.1 气缸盖拆装与检查", "[评估要素]", "◎1.1.1准备工作")
        ]
    }
]

def get(query, shot=3):
    prompt = """请将用户的船舶轮机评估问题拆解为知识图谱查询结构。

严格输出要求（非常重要）：
1) 只输出一个有效的 JSON 对象，不要添加任何说明性文本或额外注释。
2) JSON 必须包含两个顶级字段："divided"（字符串数组）和 "graph"（三元组数组）。
3) "graph" 必须是一个嵌套数组，内层条目为长度为 3 的数组： [head, relation, tail]，例如： ["主机", "[需考核科目]", "UNKNOWN"]。
4) 关系必须使用以下集合之一："[需考核科目]","[包含评估任务]","[评估要素]","[评价标准]","[具体适任要求]"。
5) 请确保输出为合法 JSON，可直接用 `json.loads()` 解析。

示例（请严格遵循此结构并仅返回类似结构的 JSON）：
{
    "divided": ["某任务的拆分项1", "某任务的拆分项2"],
    "graph": [
        ["大管轮", "[需考核科目]", "UNKNOWN 评估科目"],
        ["UNKNOWN 评估科目", "[包含评估任务]", "UNKNOWN 评估任务"]
    ]
}

现在请处理：
"""
    for i in range(min(shot, len(examples))):
        ex = examples[i]
        prompt += f"\n示例 {i+1}:\n"
        import json
        res_example = {
            "divided": ex["divided"],
            "graph": [[h, r, t] for h, r, t in ex["graph"]]
        }
        prompt += json.dumps(res_example, ensure_ascii=False, indent=4) + "\n"

    prompt += f"\n现在请处理：\nquery: '{query}'\n\n请仅返回符合上述 JSON 格式的单一对象。"
    return prompt